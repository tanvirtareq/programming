#include<bits/stdc++.h>

using namespace std;

int main()
{
    string st="ab";
    next_permutation(st.begin(), st.end());
    next_permutation(st.begin(), st.end());
    cout<<st<<endl;

    return 0;
}

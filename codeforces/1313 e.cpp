#include<bits/stdc++.h>
#define ll long long
using namespace std;

void fnd(string a, ll aa)
{
	for(int i=0;i<a.size();i++)
	{
		aa
	}

}

int main()
{
	int n, m;
	string a, b, s;
	cin>>n>>m>>a>>b>>s;

	fnd(a, aa);
	reverse(b.begin(), b.end());
	fnd(b, ba);
	fnd(s, sa);
	reverse(s.begin(), s.end());
	fnd(s, sra);

	fora(aa , sa
	fora(ba, sra);



	return 0;
}
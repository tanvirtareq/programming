#include<bits/stdc++.h> 
int main(int argc, char const *argv[])
{
	/* code */
	freopen("input.txt", "r", stdin);
	freopen("outout.txt", "w", stdout);
    int faltu;
    while(cin>>faltu)
        cout<<faltu;

    return 0;
}
#include<bits/stdc++.h>

using namespace std;
int main()
{
    string r, l;
    char f;
    int n, k;
    cin>>n>>k;
    char ch;
    cin>>ch;
    f=ch;
    r.push_back(ch);
    n--;
    while(n--)
    {
        cin>>ch;
        if(ch==f)
        {
            l.push_back(ch);

        }
        if()

    }

    return 0;
}


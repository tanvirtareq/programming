#include<bits/stdc++.h>

using namespace std;

struct mdt{
int penalty;
int solved;
};

int main()
{
    vector<map<string, mdt> >v;

    map<string, mdt> mp;

    int t;
    cin>>t;
    cin.ignore();
    cin.ignore();
    while(t--)
    {
        string x;
        char jv;
        cin>>x;
        cout<<x.size();
        if(x.size()==0) break;

    }

    return 0;
}

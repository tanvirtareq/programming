#include<bits/stdc++.h>

using namespace std;

typedef long long unsigned ull;

ll

int main()
{
    ull l, k;

    while(scanf("%llu%llu",&l,&k)!=EOF)
    {
        if(l==0&&k==0) break;

        llu d=check(l, k);

    }

    return 0;
}
